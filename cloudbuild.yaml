steps:
  - name: gcr.io/cloud-builders/git
    args:
      - clone
      - --branch=master
      - https://github.com/shahakash277/idurar-erp-crm.git
    dir: /workspace

  - name: gcr.io/cloud-builders/gcloud
    entrypoint: bash
    args:
      - -c
      - |
        echo "📦 Copying docker-compose.yml to VM..."
        gcloud compute scp docker-compose.yml crm-demo-github:~/docker-compose.yml --zone=us-central1-c

        echo "📥 Copying latest code to VM..."
        gcloud compute scp --recurse /workspace/idurar-erp-crm/ crm-demo-github:~/idurar-erp-crm --zone=us-central1-c

        echo "🚀 Deploying with docker-compose on VM..."
        gcloud compute ssh crm-demo-github --zone=us-central1-c --command="
          cd ~/idurar-erp-crm &&
          docker-compose -f ~/docker-compose.yml pull &&
          docker-compose -f ~/docker-compose.yml up -d
        "

serviceAccount: "cloud-build-service-account@cqa-portal-server-staging.iam.gserviceaccount.com"
